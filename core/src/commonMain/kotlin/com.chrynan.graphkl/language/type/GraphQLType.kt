package com.chrynan.graphkl.language.type

/**
 * An interface that represents a GraphQL Type. This is the base interface for all GraphQL Types (object, scalar,
 * interface, etc) and can be useful for scoping.
 */
interface GraphQLType